{
    "id": "3933420c-5028-40d7-9eae-2f17f5484e17",
    "name": "compile-type-script",
    "friendlyName": "Compile TypeScript",
    "description": "Compile TypeScript",
    "helpMarkDown": "Having Problems? Please <a href='https://github.com/BoolBySigma/FetchBuildArtifact/issues' target='_blank'>create an issue on our Github</a> and we will try to help you.",
    "category": "Build",
    "author": "Bool (by Sigma)",
    "version": {
        "Major": 0,
        "Minor": 0,
        "Patch": 1
    },
    "instanceNameFormat": "Compile TypeScript",
    "inputs": [
        {
            "name": "compileType",
            "type": "pickList",
            "label": "Compile",
            "defaultValue": "1",
            "options": {
                "1": "Project",
                "2": "Files"
            },
            "required": true,
            "helpMarkDown": "Path to a valid <code>tsconfig.json</code> file or directory containing the <code>tsconfig.json</code> file. Leaving it blank defaults to source root directory and is equal to using <code>$(Build.SourcesDirectory)</code>."
        },
        {
            "name": "projectPath",
            "type": "filePath",
            "label": "Project Path",
            "defaultValue": "",
            "required": false,
            "visibleRule": "compileType=1",
            "helpMarkDown": "Path to a valid <code>tsconfig.json</code> file or directory containing the <code>tsconfig.json</code> file. Leaving it blank defaults to source root directory and is equal to using <code>$(Build.SourcesDirectory)</code>."
        },
        {
            "name": "filesPath",
            "type": "filePath",
            "label": "Files Path",
            "defaultValue": "",
            "required": false,
            "visibleRule": "compileType=2",
            "helpMarkDown": "Path to a valid <code>tsconfig.json</code> file or directory containing the <code>tsconfig.json</code> file. Leaving it blank defaults to source root directory and is equal to using <code>$(Build.SourcesDirectory)</code>."
        },
        {
            "name": "files",
            "type": "string",
            "label": "Files",
            "defaultValue": "index.ts",
            "required": true,
            "visibleRule": "compileType=2",
            "helpMarkDown": "Path to a valid <code>tsconfig.json</code> file or directory containing the <code>tsconfig.json</code> file. Leaving it blank defaults to source root directory and is equal to using <code>$(Build.SourcesDirectory)</code>."
        }
    ],
    "execution": {
        "Node": {
            "target": "index.js",
            "argumentFormat": ""
        }
    }
}
